# # import db
# import SQL.db as db
# from sqlalchemy.orm import sessionmaker


# # def makeNewSession():
# # class Session:
# def getSession():
#     #new session
#     Session = sessionmaker(bind = db.engine)
#     session = Session()
#     # session = sessionmaker(bind=db.engine)
#     return session

# # def addRowToDb(_row, session):
# #     row = db.CallList(_row)
# #     session.add(row)
# #     session.commit()